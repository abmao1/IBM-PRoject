---
title: "dashboard"
---

# Dashboard

This dashboard explores an example trend in Cars vs Weight vs MPG.

## Chart Example

```{r}
library(ggplot2) 
ggplot(mtcars, aes(x = wt, y = mpg)) + geom_point() + labs(title = "MPG vs Weight", x = "Weight", y = "MPG")
```
